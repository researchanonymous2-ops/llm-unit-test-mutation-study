[1] Tarefa
Gerar testes unitários em Java (JUnit 5) para o método compress(String input) da classe com.thealgorithms.strings.StringCompression.

[2] Restrição de Escopo
Gerar casos de teste apenas para o método compress(String input).
Não criar testes para outros métodos da classe.

[3] Exemplos de Classe de Teste (Few-Shot)

Use o exemplo abaixo apenas como referência de estrutura, organização, imports e formato de uma classe de teste JUnit 5 com múltiplos casos de teste.

Não reutilize valores de entrada, resultados esperados ou a lógica dos testes apresentados.

// Exemplos

import static org.junit.jupiter.api.Assertions.assertEquals;

import com.thealgorithms.strings.StringCompression;
import org.junit.jupiter.api.Test;

public class StringCompressionFewShotExampleTest {

    @Test
    void testCompressWithRepeatedCharacters() {
        assertEquals("a3", StringCompression.compress("aaa"));
    }

    @Test
    void testCompressWithSingleCharacter() {
        assertEquals("b", StringCompression.compress("b"));
    }

    @Test
    void testCompressWithMixedCharacters() {
        assertEquals("a2b", StringCompression.compress("aab"));
    }
}

// Fim dos Exemplos

[4] Geração do Código Final (JUnit 5)
Forneça apenas o código Java completo e compilável da classe de teste.

[5] Convenções e Restrições Sintáticas
- Não incluir comentários, explicações ou texto fora do código Java;
- Nomes dos testes devem seguir o padrão: testCompress_*.

